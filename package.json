{
  "name": "sda",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd Backend && nodemon server.js\" \"cd OCHI && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "config": "^3.3.12",
    "express-oauth2-jwt-bearer": "^1.6.1",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  }
}
